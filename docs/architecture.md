```text
VanillaBase
├── docs       — документация
├── config     — управляемая конфигурация
├── scripts    — автоматизация
├── resources  — проектные ресурсы
├── backups    — локальные резервные копии
└── server     — runtime Minecraft
```

> Git хранит проект, а не состояние работающего сервера.